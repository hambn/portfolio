version: 2

updates:
  - package-ecosystem: npm
    directory: "/"
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 5
    commit-message:
      prefix: "chore(deps)"
    groups:
      # Collapse minor + patch bumps into one PR to keep the noise down;
      # major updates still arrive as individual PRs for deliberate review.
      minor-and-patch:
        applies-to: version-updates
        update-types:
          - minor
          - patch

  # Base images are pinned (node major, nginx minor); these PRs move them.
  - package-ecosystem: docker
    directories: ["/apps/web", "/apps/api"]
    schedule:
      interval: weekly
    commit-message:
      prefix: "chore(docker)"

  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    commit-message:
      prefix: "chore(ci)"
