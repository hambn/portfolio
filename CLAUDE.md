# CLAUDE.md

Guidance for working in this repo. See the full repo structure and conventions:

@.claude/structure.md

The backend lives in `api/` (Cloudflare Worker, also self-hostable on Node) —
its own guide: [.claude/api.md](.claude/api.md). Read it before touching `api/`.
