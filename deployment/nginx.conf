server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # Prerendered routes are real dirs (/blog/, /resume/...). Unknown deep links
  # fall back to 404.html, which boots the SPA — same as GitHub Pages.
  location / {
    try_files $uri $uri/ /404.html;
  }
}
